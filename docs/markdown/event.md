# 事件

这些是点歌机使用中会出现的事件。

## 弹幕事件

读取弹幕会导致以下事件。

### 有人点了一首歌

一个人通过评论点了歌，被 `reader` 读取到。

### 有人取消了

一个人通过评论取消了。

## 客户端事件

播放器客户端会触发以下事件。

### 客户端启动

客户端刚上线，与服务端建立了连接。

### 播放完成

客户端播放完一首歌，通过 Web Socket 让服务端知道了。

