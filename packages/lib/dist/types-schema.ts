// Generated by ts-to-zod
import { z } from "zod";

/**弹幕 */
export const danmakuSchema = z.object({
    timestamp: z.number(),
    message: z.string(),
    uname: z.string(),
    face: z.string(),
    ignore: z.boolean()
});

/**点歌的人，如果是备选歌单则为 `null` */
export const pickerSchema = z.string().nullable();

/**
 * 歌曲信息
 *
 * 不要相信对象引用，使用 id 来比较两个歌曲是否相同
 */
export const baseSongSchema = z.object({
    id: z.bigint(),
    title: z.string(),
    playerName: z.string(),
    info: z.unknown(),
    picker: pickerSchema
});
